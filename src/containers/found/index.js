import {connect} from 'react-redux';
import FoundContainer from "./presenter";


const mapStateToProps = (state) => {

    return  {}
};

const mapDispatchToProps = (dispatch) => {
    return {

    }
};

export default connect(mapStateToProps,mapDispatchToProps) (FoundContainer);